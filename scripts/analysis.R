# analysis.R
# Project: info-agendas
# "Information, Power, and Agendas: Drivers of (In)Attention to Climate Change"
#
# This script holds the analysis behind the manuscript: it loads the data,
# builds the analysis objects, and prepares the model / table / figure inputs
# that info-agendas.qmd sources via `source("scripts/analysis.R")`.
# Tables and figures themselves are assembled in code chunks in the .qmd;
# some results are pulled in as inline R.

# --- Packages -------------------------------------------------------------
library(dplyr)

# --- Data --------------------------------------------------------------------
# data_raw <- read.csv("data/.csv")

# --- Prep ------------------------------------------------------------------

# --- Models --------------------------------------------------------------------

# --- Objects for the manuscript --------------------------------------------
